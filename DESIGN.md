# 学习召回系统 · 设计思路

> 这份文档记录整个项目"为什么这么做"。接口契约看 [README.md](README.md)，这里讲的是理念和决策依据。

## 要解决的问题

想学的的东西很多（算法、LLVM、圆锥曲线、写作、教材……），但兴趣常随时间衰减，或被工作打断，之后就忘了之前学到哪、卡在哪。**重新进入状态的上下文成本，远高于继续学习的成本。**

目标系统要在无聊时刻（本来要刷短视频的时刻）弹出"你曾经努力投入过的问题"，复现当时的上下文，把人钩回学习状态。

## 核心洞察

1. **这不是记忆问题，是上下文恢复问题。** Anki 解决"记住知识点"，我们解决"重新进入状态"——几周后打开 LLVM，最痛的不是忘了概念，而是想不起"我当时卡在哪、下一步要干嘛"。
2. **钩子必须是"悬而未决的问题"，而且用用户自己的原话。** 未完成的任务会在脑内后台运行（蔡格尼克效应）；召回时一句自己当时写的"草，这里为什么要反向传播？"，比任何工整的总结都有冲击力。
3. **记录成本是生死线。** 记录动作超过 30 秒，系统就会因为输入枯竭而饿死。所以：agent 起草，人只确认。
4. **兴趣消退是正常的。** 系统允许主题休眠、归档，不制造愧疚感——愧疚感是这类工具用户流失的头号原因。
5. **召回是劫持碎片时间，不是争夺专注时间。** 在赶 deadline 时弹提醒就是骚扰；连续不响应要降频，不能加码轰炸。

## 系统全景

```
学习在各处发生（电脑 code agent 会话 / 纸质书 / 手机）
        │
        ├─ 电脑端：说"保存进度" → learning-checkpoint skill 起草 → 人确认 → push
        │
        └─ 线下：手机 app 录音(离线转写)/拍草稿 → attachments/ 原始材料 push
                  → 下次在电脑前说"消化归档" → learning-digest skill 补总结
        │
        ▼
GitHub 单仓（唯一事实源）
  checkpoints/   三段式：在做什么 / 下一步动作 / 悬而未决的问题
  attachments/   语音转写、草稿纸照片（消化后删语音、留照片）
        │
        ▼
Android app（召回引擎）
  定时/久未学习触发 → 弹出你当时的原话问题 + 照片 → 回忆页一键复现
        │
        ▼
恢复会话（v3：agent 读档 → 5 分钟再进入任务 → 追问 → 新 checkpoint）
```

## 关键设计决策

| 决策 | 理由 |
|------|------|
| GitHub 单仓做存储 | 免后端；每次 checkpoint 是一次 commit，天然版本历史；app 端 git clone / REST API 直接读 |
| 归档内容只有 checkpoint，学习材料留在原地 | checkpoint 里的"上下文"只存指针（路径、题号、章节），不搬运学习材料本身 |
| "约定即接口" | 目录结构、frontmatter 字段写在 README，skill 和 app 都按契约实现，改规则就是改文档 |
| skill 手动触发（"保存进度"/"消化归档"） | 自动化的是保存之后的事，不是替你决定何时存档；"我说保存"本身就是一次微型自我回顾。若总是忘记，再加会话结束 hook 兜底 |
| 消化后删语音、留照片 | 转写文本已承载音频内容；照片是召回时的记忆资产，值得永久保留 |
| app v1 只做只读召回端 | 采集已能靠"手动模拟 + digest skill"走通；召回是整个想法里唯一未验证的假设，先用最薄的版本验证"弹出旧问题能不能钩回人" |

## app 分期路线

- **v1（开发中）**：只读召回——拉取 checkpoint、课题列表、详情页、本地通知召回（久未学习 + 无聊时段）、"给我个回忆"主动入口
- **v2**：采集端——录音（系统离线转写）+ 拍照 → attachments/ push
- **v3**：app 内 LLM 恢复会话——读档生成 5 分钟再进入任务
- **v4**：行为锚定召回——检测抖音/小红书前台（Android 可用使用情况权限），在刷短视频的时刻拦截

技术栈：Android 原生 + Java + Views（用户熟悉 Java）；GitHub REST API + PAT；WorkManager 做后台召回（华为 ROM 下最稳的方案，需引导用户加电池白名单）。

## 当前状态（2026-07-29）

- ✅ 归档仓库、checkpoint 格式契约、两个 code agent skill（`learning-checkpoint` / `learning-digest`，kimi code + claude code 双端）
- ✅ digest 链路首次实测通过（书页照片 → 正确提炼三段式 checkpoint）
- ✅ app v1 UI 线框图定稿（`learning_hook/docs/ui-wireframe.excalidraw`，四界面：课题列表 / Checkpoint 详情 / 回忆 / 设置）
- 🚧 app v1 开发：M0 环境与工程骨架 → M1 拉取+列表 → M2 详情+照片 → M3 通知召回 → M4 回忆入口+设置

## 悬而未决的问题

1. **"原话"保真**：digest 实测中 checkpoint 引用了一句转写文本里不存在的话。若确认是 agent 编造，skill 需加硬规则——引用必须能在材料中逐字找到，否则标注"(agent 总结)"。整个系统的钩子力建立在"这是我自己说的话"之上，编造的原话比没有更糟。
2. **召回有效性**：v1 装机后观察——弹出旧问题，是真的会点进去做，还是划掉？这决定整个想法成不成立。
3. **通知疲劳红线**：连续几次不响应就必须降频，这个逻辑 v1 只做最简版，根据真实使用再调。
