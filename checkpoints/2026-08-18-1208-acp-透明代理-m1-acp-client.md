---
topic: ACP-透明代理-M1-ACP-client
status: active
source: code-agent
created: 2026-08-18T12:08:25+0800
---

## 在做什么
学习 **ACP(Agent Client Protocol, v1)**,目标是实现"ACP 透明代理:自己实现 ACP server 收客户端请求 + 用 ACP client 转给下游 agent"。当前在 **M1(用纯 Python 写 ACP client)**,采用"你写我辅导"模式:我的参考实现在 `reference/`,我在 `m1_client/` 从零重写。已手写 `jsonrpc.py`、`transport.py`,正在写 `client.py` 的 `session_prompt` 循环。

## 下一步动作
- 在 `m1_client/` 补齐 `client.py` 的 `session_prompt` 循环:**等响应时只认"id 匹配"的 `session/prompt` 响应**(含 `stopReason`)才退出;期间把 `session/update` 通知交给回调、把 agent 主动发来的 `session/request_permission` / `fs` / `terminal` 请求交给应答逻辑继续循环。
- 补 `mock_agent.py`(模拟流式 + 权限请求)和 `demo_client.py`(入口),跑通 `mock` 和 `codex`(后者带 `HTTPS_PROXY=http://127.0.0.1:7890`)两个验收场景。
- 完成后对照我的参考实现做逐项 review。

## 悬而未决的问题
- "传统 Python 不管多少核实际只用了一个核"——实测(按 CPU 时间)双线程 2.0s、双进程 1.0s,确认只对"纯 Python 的 CPU 计算"成立;但还没细想代理如何并发放大。
- `_send_lock` 保护"整行写入不被交错"、和 GIL(管字节码并发)是两码事——还没想过在自己的 transport 里要不要真用它。
- `session_prompt` 里"消息四类(update / request_permission / 自身响应 / 其他)"是 M1 的核心坑;暂时先满足当前工作需求。

## 上下文
- 项目:`/Users/looboo/myprojects/acp`;代码:`m1_client/`(自写),参考在 `m1_client/reference/`;任务书 `m1_client/TASK.md`
- 环境:miniconda base `/Users/looboo/miniconda3/bin/python3`(3.13.9,带 GIL);codex 0.146 走 DeepSeek,需代理 `http://127.0.0.1:7890`;跑 codex-acp 时 npx 在 `/opt/homebrew/bin`
- ACP 规范(v1): overview https://agentclientprotocol.com/protocol/v1/overview.md 、transports(stdio 帧) https://agentclientprotocol.com/protocol/v1/transports.md 、session-setup https://agentclientprotocol.com/protocol/v1/session-setup.md 、prompt-turn https://agentclientprotocol.com/protocol/v1/prompt-turn.md 、tool-calls(权限) https://agentclientprotocol.com/protocol/v1/tool-calls.md ;官方 Python SDK `agent-client-protocol`
- 已装:codex-acp 适配器 `@agentclientprotocol/codex-acp` v1.4.0(含 DeepSeek),dsh 有 `dsh-acp` 插件待装
